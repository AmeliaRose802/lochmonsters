using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class SnakeMoveScript : MonoBehaviour
{
    /*
    public float speed = 2f;
    Vector3 velocity;
    Transform snakeHead;
    List<Transform> snakeSegments;



    // Start is called before the first frame update
    void Start()
    {
        snakeSegments = new List<Transform>(GetComponentsInChildren<Transform>());

        //List of children includes self so remove it
        snakeSegments.RemoveAt(0);

        snakeHead = snakeSegments[0];
        velocity = new Vector3(speed, 0, 0);
    }

    // Update is called once per frame
    void Update()
    {
        snakeHead.position += velocity * Time.deltaTime;
    }*/
}
